---
name: parallel-tasks
description: 何时把工作拆成并行子任务（parallel_tasks 工具），以及如何写出可独立执行的子任务描述。适用于需要同时调研多个互不依赖问题的场景。
---

# 并行子任务

`parallel_tasks` 工具把多个**互不依赖的只读调研任务**派发到隔离的 pi 子进程中同时执行，
全部结束后把结果汇总回当前会话，由你完成整合。

## 什么时候用

满足**全部**条件才值得并行：

- 拆出来的子任务之间**没有先后依赖**——B 不需要等 A 的结论才能开始。
- 每个子任务都能**脱离当前会话上下文独立理解**。
- 任务本质是**调研 / 定位 / 核验**，不需要修改文件。
- 至少有 2 个子任务。只有 1 个时直接自己做，派发反而更慢。

典型场景：

- 用户明确说「同时处理 xx 和 yy」「并行看一下这几处」。
- 需要在代码库多个互不相关的区域各找一遍（例如同时排查前端调用点和后端实现）。
- 同一个结论需要多个角度交叉验证。
- 面对一个陌生模块，需要同时摸清若干条独立的线索。

## 什么时候不要用

- **任务有依赖链**：先定位再分析再改，这是顺序工作，直接做。
- **需要写文件**：子任务恒为只读，派发出去也改不了任何东西。
- **任务很小**：一次 grep 就能解决的事，派发的开销远大于收益。
- **子任务描述必须引用当前会话的上下文**才说得清——子进程看不到你的对话历史。

## 可用角色

| 角色 | 用途 | 产出 |
|------|------|------|
| `probe` | 定位取证：找到相关文件、符号、调用点 | `路径:行号` 引用清单 |
| `analyst` | 机制分析：读懂某个流程如何工作 | 带证据的机制说明 |
| `verifier` | 假设核验：判定一个具体假设是否成立 | 成立 / 不成立 / 证据不足 |

全部只读（`read`/`grep`/`find`/`ls`/受严格白名单限制的 `bash`），无法写入或编辑文件；bash 的重定向、管道、命令拼接、解释器和写入类命令都会被拦截。

## 怎么写子任务描述

子进程**看不到**你的对话历史、看不到其他子任务、也看不到彼此的结论。描述必须自包含：

- 写清楚**范围**：哪个目录、哪些文件、哪个模块。
- 写清楚**判断标准**：什么算找到了，什么算没找到。
- 给 `verifier` 的必须是**可判定的假设**，不是开放问题。

反例（子进程无法理解）：

```
role: analyst
task: 看看刚才说的那个问题在这里是不是也存在
```

正例（自包含）：

```
role: analyst
task: 分析 src/core/session-manager.ts 中 session 条目的持久化流程：
      条目何时写盘、是否有缓冲、进程异常退出时未落盘的条目会怎样。
```

## 整合阶段

子任务全部结束后，结果会连同整合要求一起回到当前会话。你需要：

1. **合并共识**——多个子任务独立得出的一致结论，可信度最高。
2. **处理冲突**——结论矛盾时以有 `路径:行号` 证据的一方为准；无法判定就明确告诉用户存在分歧，不要静默挑一个。
3. **保留存疑项**——子任务标注的「存疑 / 证据不足 / 风险」不要在整合时丢掉。
4. **落地改动**——子任务只读，此时仓库尚未有任何修改。需要动手改代码的话，由你在当前会话执行。

## 相关命令

- `/parallel-roles` —— 列出当前可用的角色定义。
